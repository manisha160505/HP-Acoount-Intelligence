# ==============================================================================
# HP ACCOUNT INTELLIGENCE PLATFORM — RECURSIVE GIT IGNORE RULES
# ==============================================================================

# Environment files (Secrets & Local Paths - DO NOT PUSH)
.env
.env.local
.env.*.local
*.env

# Raw Reference Data Files (Private / DO NOT PUSH)
Source A.xlsx
Source B.xlsx
google_news_rss_data.xlsx
11-Features-Sourcing-Reference-Anonymized.docx
Master Next.js Project Structure.pdf
Master FastAPI Project Structure.pdf
# HP product decks. Marked "HP Confidential. For use by HP or Partner with
# Customers under HP CDA only." on 159 of their 422 slides, and ~1GB of media.
# The extracted product facts live in the hp_product_knowledge collection on
# the shared cluster, never in the repository - see scripts/extract_hp_decks.py.
products/
*.pptx
*.ppt
hp_product_knowledge*.json

*.xlsx
*.xls
*.csv
*.docx
*.pdf

# Allow Seed Data for Auto-Seeding Fresh Clones
!hp-backend/seed_data/
!hp-backend/seed_data/*
!hp-backend/seed_data/**/*

# Customer Uploaded Data & Reference CSV Directories
**/data/
data/
reference_csvs/

# Recursive Dependencies & Build Outputs
**/node_modules/
**/.next/
**/out/
**/build/
**/tsconfig.tsbuildinfo

# Python Cache & Environments
**/__pycache__/
**/*.pyc
**/*.pyo
.pytest_cache/
.venv/
env/
venv/

# Media & Reference Codebase Directories
*.mp4
*.png
hp-sea-abm-dev-main/

# OS Files
.DS_Store
Thumbs.db
*.log

# VS Code debug Chrome profile
.vscode/.chrome-profile/

# code-review-graph (local knowledge graph + Claude Code MCP config, skills, instructions)
.code-review-graph/
.mcp.json
.claude/
/CLAUDE.md

# Retrieval engine
# r-engine.zip ships a live Azure key in its bundled .env; transcript.txt is a
# raw client meeting recording. Neither belongs in the repository.
r-engine.zip
transcript.txt
rag_storage/
**/rag_storage/
ingest/
.crg-venv/

# Private correspondence and bulk reference material kept locally only.
# Meeting notes under docs/meeting-notes/ are deliberately still tracked.
docs/mails*.txt
hp-project-flow.html

# Editor backups. Seven page.tsx.bak* snapshots were tracked, and the widget
# validator scanned them as if they were live UI - reporting a "Contract TBD"
# placeholder that had already been removed from page.tsx. Their history stays
# in git; only the working copies are ignored from here.
*.bak
*.bak[0-9]
*.bak[0-9][0-9]

# Standalone static lineage site + clickable replica, deployed separately to
# Vercel. Kept out of this repository: it carries a 649KB read-only capture of
# real account data (replica/assets/live.js) and is not part of the app build.
vercel app/

# Raw 220-account source data (exa, predictleads, google news, intent results,
# explorium). ~140MB of real client account data, internal only, never committed.
220 account data /

# Per-account CSVs produced from the above by scripts/split_account_data.py.
# Not committed: ~230MB / 5,942 files, and regenerable. Place the source data
# at "220 account data /" exactly as named above and run
# scripts/split_account_data.py to produce this folder locally.
220 account split csv/

# Client documents (rulebooks, decks, spreadsheets, filings) shared for the
# 220-account work. Real client material, internal only, never committed.
# Only the open-decisions list is tracked; everything else in the folder stays
# ignored.
NewDocs/*
!NewDocs/220-Open-Decisions-List.md

# Local tooling for validating the 220-account data above. The split script
# itself (scripts/split_account_data.py) is tracked so the team can regenerate
# the per-account CSVs; the validator and its baseline stay local.
scripts/split_baseline.json
scripts/validate_split_data.py

# ==============================================================================
# Local-only working folders (added 25 Sep). None of this is tracked today; the
# rules make that explicit so a stray `git add -A` cannot pick them up.
# ==============================================================================

# The delivered 220-account source data: explorium workbooks, exa, google news
# RSS, predictleads and the intent results. ~67MB of real client account data.
# Note the older "220 account data /" rule above names a folder that no longer
# exists, and carries a stray space before its slash; this is the live one.
220 accounts/

# Working archive of superseded drops and documents.
Archive/

# Project documentation kept locally rather than in the repository.
project-documentation/

# Internal delivery planning for the 220-account run. Working document, kept
# local rather than pushed.
DELIVERY_ROADMAP_220_2026-09-25.md

# Internal rule book for the 220-account run: every client rule against what
# the build actually does. Working document, kept local.
HP_220_FINAL_RULE_BOOK_2026-09-25.md
